import { Box } from "@mui/material";
import React from "react";

const Hero = () => {
  return (
    <Box px={20} py={10} sx={{ borderBottom: "1px solid gray" }}>
      Dashboard
    </Box>
  );
};

export default Hero;
